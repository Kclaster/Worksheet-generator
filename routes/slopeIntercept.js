var express = require('express');
var router = express.Router();
var connection = require('../config/connection');
var app = express();


router.post('/slope_intercept', function(req, res) {
  

module.exports = router;

/////create a todod route for personal
